[pytest]
testpaths = tests
pythonpath = collector scripts tools .
addopts = -q --strict-markers
markers =
    unit: fast isolated tests
    integration: API / file-pipeline tests
    e2e: reserved; UI lives in e2e/ (Playwright)
filterwarnings =
    ignore::DeprecationWarning
